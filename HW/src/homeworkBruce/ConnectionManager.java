package homeworkBruce;

public class ConnectionManager {

	public static void main(String[] args) {
		Connection con1= Connection.makeConnect();
		Connection con2= Connection.makeConnect();
		Connection con3= Connection.makeConnect();
		Connection con4= Connection.makeConnect();

	}

}
